
#include <jackshit.h>

void core::bit0a( void )
{
	ZUPDATE( ( regs.b.a & ( 1 << 0 ) ) );
	regs.w.pc++;
	return 2;
}

void core::bit0b( void )
{
	ZUPDATE( ( regs.b.b & ( 1 << 0 ) ) );
	regs.w.pc++;
	return 2;
}

void core::bit0c( void )
{
	ZUPDATE( ( regs.b.c & ( 1 << 0 ) ) );
	regs.w.pc++;
	return 2;
}

void core::bit0d( void )
{
	ZUPDATE( ( regs.b.d & ( 1 << 0 ) ) );
	regs.w.pc++;
	return 2;
}

void core::bit0e( void )
{
	ZUPDATE( ( regs.b.e & ( 1 << 0 ) ) );
	regs.w.pc++;
	return 2;
}

void core::bit0h( void )
{
	ZUPDATE( ( regs.b.h & ( 1 << 0 ) ) );
	regs.w.pc++;
	return 2;
}

void core::bit0l( void )
{
	ZUPDATE( ( regs.b.l & ( 1 << 0 ) ) );
	regs.w.pc++;
	return 2;
}

void core::bit1a( void )
{
	ZUPDATE( ( regs.b.a & ( 1 << 1 ) ) );
	regs.w.pc++;
	return 2;
}

void core::bit1b( void )
{
	ZUPDATE( ( regs.b.b & ( 1 << 1 ) ) );
	regs.w.pc++;
	return 2;
}

void core::bit1c( void )
{
	ZUPDATE( ( regs.b.c & ( 1 << 1 ) ) );
	regs.w.pc++;
	return 2;
}

void core::bit1d( void )
{
	ZUPDATE( ( regs.b.d & ( 1 << 1 ) ) );
	regs.w.pc++;
	return 2;
}

void core::bit1e( void )
{
	ZUPDATE( ( regs.b.e & ( 1 << 1 ) ) );
	regs.w.pc++;
	return 2;
}

void core::bit1h( void )
{
	ZUPDATE( ( regs.b.h & ( 1 << 1 ) ) );
	regs.w.pc++;
	return 2;
}

void core::bit1l( void )
{
	ZUPDATE( ( regs.b.l & ( 1 << 1 ) ) );
	regs.w.pc++;
	return 2;
}

void core::bit2a( void )
{
	ZUPDATE( ( regs.b.a & ( 1 << 2 ) ) );
	regs.w.pc++;
	return 2;
}

void core::bit2b( void )
{
	ZUPDATE( ( regs.b.b & ( 1 << 2 ) ) );
	regs.w.pc++;
	return 2;
}

void core::bit2c( void )
{
	ZUPDATE( ( regs.b.c & ( 1 << 2 ) ) );
	regs.w.pc++;
	return 2;
}

void core::bit2d( void )
{
	ZUPDATE( ( regs.b.d & ( 1 << 2 ) ) );
	regs.w.pc++;
	return 2;
}

void core::bit2e( void )
{
	ZUPDATE( ( regs.b.e & ( 1 << 2 ) ) );
	regs.w.pc++;
	return 2;
}

void core::bit2h( void )
{
	ZUPDATE( ( regs.b.h & ( 1 << 2 ) ) );
	regs.w.pc++;
	return 2;
}

void core::bit2l( void )
{
	ZUPDATE( ( regs.b.l & ( 1 << 2 ) ) );
	regs.w.pc++;
	return 2;
}

void core::bit3a( void )
{
	ZUPDATE( ( regs.b.a & ( 1 << 3 ) ) );
	regs.w.pc++;
	return 2;
}

void core::bit3b( void )
{
	ZUPDATE( ( regs.b.b & ( 1 << 3 ) ) );
	regs.w.pc++;
	return 2;
}

void core::bit3c( void )
{
	ZUPDATE( ( regs.b.c & ( 1 << 3 ) ) );
	regs.w.pc++;
	return 2;
}

void core::bit3d( void )
{
	ZUPDATE( ( regs.b.d & ( 1 << 3 ) ) );
	regs.w.pc++;
	return 2;
}

void core::bit3e( void )
{
	ZUPDATE( ( regs.b.e & ( 1 << 3 ) ) );
	regs.w.pc++;
	return 2;
}

void core::bit3h( void )
{
	ZUPDATE( ( regs.b.h & ( 1 << 3 ) ) );
	regs.w.pc++;
	return 2;
}

void core::bit3l( void )
{
	ZUPDATE( ( regs.b.l & ( 1 << 3 ) ) );
	regs.w.pc++;
	return 2;
}

void core::bit4a( void )
{
	ZUPDATE( ( regs.b.a & ( 1 << 4 ) ) );
	regs.w.pc++;
	return 2;
}

void core::bit4b( void )
{
	ZUPDATE( ( regs.b.b & ( 1 << 4 ) ) );
	regs.w.pc++;
	return 2;
}

void core::bit4c( void )
{
	ZUPDATE( ( regs.b.c & ( 1 << 4 ) ) );
	regs.w.pc++;
	return 2;
}

void core::bit4d( void )
{
	ZUPDATE( ( regs.b.d & ( 1 << 4 ) ) );
	regs.w.pc++;
	return 2;
}

void core::bit4e( void )
{
	ZUPDATE( ( regs.b.e & ( 1 << 4 ) ) );
	regs.w.pc++;
	return 2;
}

void core::bit4h( void )
{
	ZUPDATE( ( regs.b.h & ( 1 << 4 ) ) );
	regs.w.pc++;
	return 2;
}

void core::bit4l( void )
{
	ZUPDATE( ( regs.b.l & ( 1 << 4 ) ) );
	regs.w.pc++;
	return 2;
}

void core::bit5a( void )
{
	ZUPDATE( ( regs.b.a & ( 1 << 5 ) ) );
	regs.w.pc++;
	return 2;
}

void core::bit5b( void )
{
	ZUPDATE( ( regs.b.b & ( 1 << 5 ) ) );
	regs.w.pc++;
	return 2;
}

void core::bit5c( void )
{
	ZUPDATE( ( regs.b.c & ( 1 << 5 ) ) );
	regs.w.pc++;
	return 2;
}

void core::bit5d( void )
{
	ZUPDATE( ( regs.b.d & ( 1 << 5 ) ) );
	regs.w.pc++;
	return 2;
}

void core::bit5e( void )
{
	ZUPDATE( ( regs.b.e & ( 1 << 5 ) ) );
	regs.w.pc++;
	return 2;
}

void core::bit5h( void )
{
	ZUPDATE( ( regs.b.h & ( 1 << 5 ) ) );
	regs.w.pc++;
	return 2;
}

void core::bit5l( void )
{
	ZUPDATE( ( regs.b.l & ( 1 << 5 ) ) );
	regs.w.pc++;
	return 2;
}

void core::bit6a( void )
{
	ZUPDATE( ( regs.b.a & ( 1 << 6 ) ) );
	regs.w.pc++;
	return 2;
}

void core::bit6b( void )
{
	ZUPDATE( ( regs.b.b & ( 1 << 6 ) ) );
	regs.w.pc++;
	return 2;
}

void core::bit6c( void )
{
	ZUPDATE( ( regs.b.c & ( 1 << 6 ) ) );
	regs.w.pc++;
	return 2;
}

void core::bit6d( void )
{
	ZUPDATE( ( regs.b.d & ( 1 << 6 ) ) );
	regs.w.pc++;
	return 2;
}

void core::bit6e( void )
{
	ZUPDATE( ( regs.b.e & ( 1 << 6 ) ) );
	regs.w.pc++;
	return 2;
}

void core::bit6h( void )
{
	ZUPDATE( ( regs.b.h & ( 1 << 6 ) ) );
	regs.w.pc++;
	return 2;
}

void core::bit6l( void )
{
	ZUPDATE( ( regs.b.l & ( 1 << 6 ) ) );
	regs.w.pc++;
	return 2;
}

void core::bit7a( void )
{
	ZUPDATE( ( regs.b.a & ( 1 << 7 ) ) );
	regs.w.pc++;
	return 2;
}

void core::bit7b( void )
{
	ZUPDATE( ( regs.b.b & ( 1 << 7 ) ) );
	regs.w.pc++;
	return 2;
}

void core::bit7c( void )
{
	ZUPDATE( ( regs.b.c & ( 1 << 7 ) ) );
	regs.w.pc++;
	return 2;
}

void core::bit7d( void )
{
	ZUPDATE( ( regs.b.d & ( 1 << 7 ) ) );
	regs.w.pc++;
	return 2;
}

void core::bit7e( void )
{
	ZUPDATE( ( regs.b.e & ( 1 << 7 ) ) );
	regs.w.pc++;
	return 2;
}

void core::bit7h( void )
{
	ZUPDATE( ( regs.b.h & ( 1 << 7 ) ) );
	regs.w.pc++;
	return 2;
}

void core::bit7l( void )
{
	ZUPDATE( ( regs.b.l & ( 1 << 7 ) ) );
	regs.w.pc++;
	return 2;
}
